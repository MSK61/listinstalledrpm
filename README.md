listinstalledrpm
================

The script summarizes the currently installed RPM packages from the given yum log.

The script reads in the provided yum log file and infers which packages are still installed after passing through the log operations. The results are dumped to an output file.
